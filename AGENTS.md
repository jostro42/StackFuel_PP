# Working Agreements

- Treat requests to "check", "review", "inspect", or "explain" as read-only. Report findings and suggested fixes without modifying files.
- Only edit files when I explicitly request changes.
- If my intent is ambiguous, ask before editing.
- Do not execute notebook cells that save or overwrite data
  without explicit authorization.

# Project context

- This project analyzes Metacritic game review data.
- Notebooks live in notebooks/.
- Shared Python code lives in src/.
- Raw datasets live in data/raw/.
- Reformatted or otherwise processed datasets live in data/processed/.
